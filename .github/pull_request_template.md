## What

<!-- One or two sentences. -->

## Why

<!-- For rule changes: name the official MCP source you read and quote the line
     that justifies the change. Every rule must trace to an official
     specification page, changelog entry or SEP. -->

## Checklist

- [ ] `npm run verify` passes (typecheck + lint + test + build)
- [ ] Rule changes include a fixture and a **negative** test (something similar
      that must not fire)
- [ ] `docs/rule-matrix.md` and the README rule table updated if rules changed
- [ ] `CHANGELOG.md` entry added
- [ ] No deprecation is described as a breaking change
